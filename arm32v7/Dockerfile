FROM arm32v7/node:dubnium-buster

RUN apt-get update && apt-get install -y --no-install-recommends build-essential libsecret-1-dev fakeroot rpm libx11-dev libxkbfile-dev && rm -rf /var/lib/apt/list/*
